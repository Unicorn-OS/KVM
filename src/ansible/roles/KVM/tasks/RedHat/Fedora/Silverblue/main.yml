- name: Install core KVM packages
  community.general.rpm_ostree_pkg:
    name: "{{ item }}"
    state: present
  loop:
  - virt-install
  - virt-viewer
  - libvirt
  notify:
  - Reboot
  - start the libvirtd service
  become: true
