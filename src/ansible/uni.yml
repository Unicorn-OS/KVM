- name: install KVM & Virt-Manager
  hosts: localhost #, node
  vars:
    optional_packages: true

  roles:
  - dev/ansible-role-KVM
  - dev/ansible-role-Virt-Manager
#  - UniInc.KVM
#  - UniInc.Virt-Manager
